version: 2
sources:
  - name: raw
    schema: gz_raw_data
    tables:
      - name: sales
        identifier: raw_gz_sales
        description: Greenweez sold products, one row per products_id found in each orders_id 
        columns:
          - name: date date 
          description: Date of the order
          - name: orders id
          description: Foreign key corresponding to the order number
          - name: pdt_id
        description: Foreign key corresponding to the product ID
          - name: revenue
        description: total turnover generated by the sales of this product in this order
          - name: quantity
        description: number of similar products included in this order 
  tests:
    - unique:
        column name: "(orders id I| '-' I pdt id)"
  - name: product
          identifier: raw_gz_product
  - name: ship
         identifier: raw_gz_ship
